import { Student } from '../student';

export class Enquiries {
    student: Student;
    month: string;
    leadSource: string;
    schoolIntakeYear: string;
    campusId: number;
    rankingScore: number;
}