import { Component } from '@angular/core';
import { IntegerWidget } from 'ngx-schema-form';

@Component({
    selector: 'integer-widget',
    templateUrl: './integer-widget.html'
})
export class MatIntegerWidget extends IntegerWidget { }
